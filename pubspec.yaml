name: teleonco_capacita
description: "Dashboard de capacitação de profissionais da saúde."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: ^1.1.1
  provider: ^6.0.5
  intl: ^0.20.2
  excel: ^4.0.6
  http: ^1.6.0      # ← CORRETO
  csv: ^6.0.0
  pdf: ^3.10.1
  printing: ^5.11.0
  path_provider: ^2.0.15
  firebase_core: ^4.2.1
  cloud_firestore: ^6.1.0
  flutter_map: ^8.2.2
  latlong2: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/geo/rs_municipios.geojson
    
